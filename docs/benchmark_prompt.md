# Compiler live benchmark

Four runs: 6 problems -> 2 -> 2 -> 0.
Every failure became a feature, a teaching message, or a gate.


Run 5 (post-L1 lint): compiler emits target_prevalence [0.15,
0.20] as taught; intercept again miscalibrated (realizes 67% -
same deficit as run 3). Semantic lint catches it PRE-RENDER with
intercept advice; L5 flags the ignored name pool. The run-3
human-discovery loop is now a machine reflex. Mistral verdict:
translation excellent, arithmetic unreliable - linter compensates
permanently.
