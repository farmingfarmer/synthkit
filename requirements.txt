# The dependencies now live in pyproject.toml, so `pip install -e .`
# installs them and this file no longer holds a second copy of the
# list. Two places to keep in step is how they come to disagree.
#
# It is kept, and kept working, because `docs/WINDOWS.md` section 2
# names it and that ritual is pasted by hand into a terminal that has
# mangled commands three times. Removing the file would break a
# documented step on the machine that is hardest to debug from here.
#
# `pip install -r requirements.txt` still installs everything needed;
# it now does so by installing the package itself.
.
